<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/action_search"
        android:title="@string/search"
        android:icon="@drawable/ic_search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="collapseActionView|always"/>

    <item android:id="@+id/action_search_by"
        android:title="@string/search_by"
        android:icon="@drawable/ic_action_title"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_manually_add"
        android:title="@string/manually_add"
        android:icon="@drawable/ic_editar"
        app:showAsAction="never" />

</menu>
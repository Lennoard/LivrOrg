<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <item android:title="@string/my_collection">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/nav_reading"
                    android:icon="@drawable/ic_book"
                    android:title="@string/reading_state_reading" />
                <item
                    android:id="@+id/nav_wish"
                    android:icon="@drawable/ic_lista_desejos"
                    android:title="@string/reading_state_wish" />
                <item
                    android:id="@+id/nav_on_hold"
                    android:icon="@drawable/ic_on_hold"
                    android:title="@string/reading_state_on_hold" />
                <item
                    android:id="@+id/nav_dropped"
                    android:icon="@drawable/ic_dropped"
                    android:title="@string/reading_state_dropped" />
                <item
                    android:id="@+id/nav_finished"
                    android:icon="@drawable/ic_finished"
                    android:title="@string/reading_state_finished" />
                <item
                    android:id="@+id/nav_favorites"
                    android:icon="@drawable/ic_favorite"
                    android:title="@string/favorites" />
            </group>
        </menu>
    </item>

    <item android:title="@string/find">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/nav_search"
                    android:icon="@drawable/ic_search"
                    android:title="@string/search" />
            </group>
        </menu>

    </item>

</menu>
